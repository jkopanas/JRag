const resolvers = {
  Query: {
    ingestJob: async (parent, { userId, id }, context) => {
      // TODO: Implement actual data fetching logic
      return null
    }
  }
}

module.exports = resolvers
